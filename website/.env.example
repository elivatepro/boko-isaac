# This file is a placeholder for environment variables required to run this project.
# Copy it to .env.local and fill with your own values.

# Password for protected pages/routes
PAGE_ACCESS_PASSWORD=password

# Supabase credentials
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-public-anon-key
# Keep this secret (server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Vercel Blob Storage (get this from Vercel Dashboard → Storage → your blob store → .env.local tab)
BLOB_READ_WRITE_TOKEN=your-vercel-blob-token
